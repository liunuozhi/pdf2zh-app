/**
 * File list component initialization.
 * The file list is dynamically updated in the main renderer index.ts.
 */
export function initFileList() {
  // File list is managed dynamically; placeholder for future queue logic.
}
